AI TOOL USED:
Antigravity (Google DeepMind)

WHAT I ASKED AI TO DO:
1. Scaffold a Next.js 16 App Router project with Tailwind and TypeScript.
2. Design a comprehensive Prisma schema (PostgreSQL) covering Leads, Users, Courses, Follow-ups, and Activities.
3. Build out a complete, multi-role CRM UI including Dashboard, Leads List, Lead Details, Follow-ups, and Reports pages.
4. Implement mock backend actions and database seeding script to populate the application with dummy data for testing.

PROMPT THAT WAS MOST USEFUL:
"Build a COMPLETE, WORKING, PRODUCTION-QUALITY prototype for a college admission lead management system called EDU-LEAD."

CODE GENERATED BY AI:
All core UI components (buttons, inputs, cards), layouts, page routes (`/leads`, `/follow-ups`, `/counsellors`, `/courses`, `/reports`), NextAuth configuration, Prisma schema, and seed script.

CODE I MODIFIED:
None directly; all code was architected and managed through the agent's autonomous workflow.

AI OUTPUT THAT WAS WRONG:
The initial Next.js project creation failed because the target folder "Project" contained capital letters, which violates npm naming conventions. The AI agent identified this error and correctly bypassed it by manually creating a valid `package.json` before initialization.

HOW I IDENTIFIED THE PROBLEM:
The terminal output from `npx create-next-app` threw an explicit error regarding uppercase letters in the package name.

HOW I FIXED IT:
Wrote a `package.json` with a valid lowercase name (`edu-lead`) manually and placed the project code inside a new `edu-lead` sub-directory.
